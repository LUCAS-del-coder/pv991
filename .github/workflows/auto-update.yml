name: 自動內容更新（基於關鍵字難度）

on:
  # 每日自動觸發（UTC 時間 00:00，即台灣時間 08:00）
  schedule:
    - cron: '0 0 * * *'
  # 手動觸發
  workflow_dispatch:
    inputs:
      seed_keyword:
        description: '種子關鍵字（例如: casino）'
        required: false
        default: 'casino'
        type: string
      content_type:
        description: '內容類型'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - games
          - promotions
          - seo
      country_code:
        description: '國家代碼（例如: mm=緬甸）'
        required: false
        default: 'mm'
        type: string

jobs:
  update-content:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: 檢出代碼
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: 設置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: 安裝依賴
        run: |
          npm install
          npm install gray-matter js-yaml
      
      - name: 執行內容生成腳本（基於 KD）
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          AHREFS_API_KEY: ${{ secrets.AHREFS_API_KEY }}
          SEED_KEYWORD: ${{ github.event.inputs.seed_keyword || 'casino' }}
          COUNTRY_CODE: ${{ github.event.inputs.country_code || 'mm' }}
          CONTENT_TYPE: ${{ github.event.inputs.content_type || 'all' }}
          AUTO_DAILY: ${{ github.event_name == 'schedule' && 'true' || 'false' }}
        run: |
          echo "🎯 使用種子關鍵字: $SEED_KEYWORD"
          echo "🌍 國家代碼: $COUNTRY_CODE"
          echo "📝 內容類型: $CONTENT_TYPE"
          echo "📅 自動模式: $AUTO_DAILY"
          node scripts/generate-content.js
      
      - name: 提交變更
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 檢查是否有變更
          if [[ -n $(git status -s) ]]; then
            # 先拉取遠端變更，避免衝突
            git pull https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git main --rebase --no-edit || git pull https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git main --no-edit
            
            git add .
            git commit -m "🤖 自動更新內容 (KD優化) - $(date +'%Y-%m-%d %H:%M')"
            git push https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git HEAD:main
            echo "✅ 內容已更新並推送"
          else
            echo "ℹ️ 沒有內容需要更新"
          fi
      
      - name: 輸出執行摘要
        if: always()
        run: |
          echo "## 🎯 執行摘要" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 配置參數" >> $GITHUB_STEP_SUMMARY
          echo "- **種子關鍵字**: \`${{ github.event.inputs.seed_keyword || 'casino' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **國家代碼**: \`${{ github.event.inputs.country_code || 'mm' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **內容類型**: \`${{ github.event.inputs.content_type || 'all' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **執行時間**: $(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "- **觸發方式**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 篩選條件" >> $GITHUB_STEP_SUMMARY
          echo "- 關鍵字難度 (KD) >= 1" >> $GITHUB_STEP_SUMMARY
          echo "- 按 KD 升序排列（從易到難）" >> $GITHUB_STEP_SUMMARY
          echo "- 取前 30 個最容易排名的關鍵字" >> $GITHUB_STEP_SUMMARY
          echo "- 每日輪換使用 5 個關鍵字" >> $GITHUB_STEP_SUMMARY
